--- Make noEmitError:true in tsconfig.json. And run command tsc -p tsconfig.json
--noEmitOnError : Will not create JS file if there will be errors in .ts file.

After setting up above congiguration,
Command to run
tsc -p tsconfig.json